---
"termost": major
---

Replace `helpers.message` and `helpers.format` with a unified `createLogger` API backed by `@clack/prompts`, and remove the `helpers` namespace.

Breaking changes and migration path documented in the [MIGRATION.md](https://github.com/adbayb/termost/blob/main/MIGRATION.md).
